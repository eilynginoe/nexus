---

---

<header>
    <section class="logoContainer">
        <a href="/" class="logo-link">
            <div class="image-container">
                <img
                    class="imgLogo"
                    src="./img/logo.webp"
                    alt="Logo de Nexus"
                />
                <img class="imgLogo-e" src="./img/e.svg" alt="Imagen E" />
                <img class="imgLogo-i" src="./img/i.svg" alt="Imagen I" />
            </div>
        </a>
    </section>
    <div class="hamburger" id="hamburger">
        <span></span>
        <span></span>
        <span></span>
    </div>
    <ul class="nav-links">
        <li><a href="/">INICIO</a></li>
        <li><a href="/productos">PRODUCTOS</a></li>
        <li><a href="/acerca-de">ACERCA DE</a></li>
        <li><a href="/contacto">CONTACTO</a></li>
    </ul>
</header>

<script src="/services/script.js" is:inline></script>

<style>
    /* Basic header styling */
    header {
        position: -webkit-sticky; /* Compatibilidad con Safari */
        position: sticky;
        top: 0;
        width: 100%;
        height: 55px;
        background-color: white;
        display: flex;
        flex-flow: row nowrap;
        align-items: center;
        justify-content: space-between;
        margin: 0; /* Asegúrate de que no haya margen */
        padding: 0 20px; /* El padding lateral puede permanecer */
        font-family: "Dosis", sans-serif;
        z-index: 1000;
        transition:
            transform 0.3s ease,
            height 0.3s ease,
            padding 0.3s ease;
        box-sizing: border-box;
        box-shadow: 0px 6px 12px rgba(0, 0, 0, 0.1); /* Mantiene la sombra */
    }

    /* Clase que se añadirá al hacer scroll */
    .header-scrolled {
        height: 45px;
        padding: 0 10px; /* Reduce el padding solo lateralmente */
    }

    .logoContainer {
        height: auto;
    }

    .image-container {
        display: flex;
        height: 100%;
        gap: 5px;
    }

    /* Propiedades comunes a todas las imágenes */
    .imgLogo,
    .imgLogo-e,
    .imgLogo-i {
        margin: 5px 0; /* Elimina márgenes adicionales si los hay */
        padding: 0; /* Elimina cualquier relleno adicional */
        height: 40px;
    }

    /* Logo hover effect */
    .logo-link {
        display: flex;
        flex-direction: row;
        align-items: center;
        gap: 0.5rem;
        text-decoration: none;
        padding: 5px;
    }

    .logo-link:hover .imgLogo {
        transform: scale(1.1) rotate(10deg);
        filter: drop-shadow(0 4px 4px rgba(0, 0, 0, 0.3));
    }

    .logo-link:hover .color-sweet {
        color: #551503;
    }

    .logo-link:hover .color-delight {
        color: #e7b16c;
    }

    .nav-links {
        width: 100%;
        display: none; /* Se oculta inicialmente */
        flex-flow: column nowrap; /* Cambiado a 'nowrap' para que todo esté en una sola columna */
        background: white;
        text-align: center;
        margin: 0 auto;
        position: absolute; /* Se posiciona de manera absoluta para que aparezca debajo del header */
        top: 50px; /* Ajusta este valor si la altura del header cambia */
        left: 0;
    }

    .nav-links.active {
        display: flex; /* Se muestra cuando el menú está activo */
    }

    .nav-links li a.active-link {
    border-bottom: 2px solid #a40000;
}

    .nav-links li {
        width: 100%; /* Cada elemento ocupa el ancho completo */
        padding: 10px 0; /* Añade un poco de espacio alrededor de cada enlace */
        transition: background 0.3s ease;
        display: flex;
        align-items: center;
        justify-content: center;
    }

    .nav-links a {
        text-decoration: none;
        color: #000000;
        font-weight: bold;
        display: block;
        width: 100%;
        height: 100%;
        padding: 10px;
        text-align: center;
        line-height: 1.5;
        box-sizing: border-box;
        transition: color 0.3s ease;
    }

    .nav-links a:hover {
        background-color: #e4e4e4; /* CAMBIAR tono del hover */
        color: black; /* Texto blanco para contraste */
    }

    .hamburger {
        display: flex;
        flex-direction: column;
        cursor: pointer;
        gap: 5px;
    }

    .hamburger span {
        display: block;
        width: 25px;
        height: 3px;
        background-color: #505050; /*CAMBIAR AL GUSTO*/
        border-radius: 3px;
        transition: all 0.3s ease;
    }

    
    @media (min-width: 768px) {
    .nav-links {
        display: flex; /* Muestra los enlaces de navegación en pantallas grandes */
        flex-direction: row; /* Coloca los enlaces en una fila */
        position: static; /* Restablece la posición para evitar la superposición */
        width: auto; /* Ajusta el ancho para adaptarse al contenido */
        background: none; /* Elimina el fondo si no es necesario */
        text-align: left; /* Alinea el texto a la izquierda */
        margin: 0;
        padding: 0;
    }

    .nav-links li {
        width: auto; /* Ajusta el ancho automáticamente */
        padding: 0 20px 0 0; /* Elimina el padding */
    }

    .nav-links a {
        padding: 0 15px; /* Espacio alrededor de los enlaces */
    }

    .hamburger {
        display: none; /* Oculta el icono de hamburguesa en pantallas grandes */
    }
}
</style>
